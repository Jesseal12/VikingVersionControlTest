using UnityEngine;

public class UselessScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("this script is useless");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
